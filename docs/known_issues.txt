1) allocating page directories with slab results in tripple fault
2) can't run a process because pmm allocates page in 4kb granularty, resulting in
    a page fault because reserved bits in page table entries are set.